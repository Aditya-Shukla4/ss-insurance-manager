{
  // Node + React part
  "deno.enable": false,

  // Enable Deno only inside Supabase Functions folder
  "deno.enablePaths": ["supabase/functions"],

  // Optional but helpful
  "deno.lint": true,
  "deno.unstable": ["kv", "http", "net", "ffi"],

  // Formatter setup
  "[typescript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
